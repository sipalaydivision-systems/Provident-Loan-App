{
  "name": "provident-loan-app",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "build": "cd frontend && npm install && npm run build",
    "start": "cd frontend && npm install && npm run start",
    "dev": "cd frontend && npm run dev"
  }
}
